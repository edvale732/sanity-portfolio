{
  "name": "sanity-portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Sanity Portfolio",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run --parallel build"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "dependencies": {
    "@sanity/icons": "^5.2.2",
    "@sanity/image-url": "^2.1.1"
  }
}